const styles = (theme) =>({
  myGrid: {
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 10
  },
  listEmployee: {
    minWidth: 200
  },
  profile: {
    minWidth: 400,
    minHeight: 400,
    textAlign: 'left',
  }
});

export default styles;