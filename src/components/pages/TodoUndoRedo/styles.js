const styles = (theme) =>({
  myGrid: {
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 10
  },
  form: {
    minWidth: 400,
    background: '#fff',
    padding: '10px'
  },
});

export default styles;